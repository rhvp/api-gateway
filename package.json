{
  "name": "bg-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && node ./dist/index.js",
    "start:dev": "npm run build && node ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && tsc -p tsconfig.json",
    "clean": "rm -rf ./build",
    "db:migrate": "npm run build && npx sequelize-cli db:migrate",
    "db:migrate:undo": "npm run build && npx sequelize-cli db:migrate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0",
    "@types/express": "^5.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "bcryptjs": "^3.0.2",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "pg": "^8.15.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.7"
  }
}
